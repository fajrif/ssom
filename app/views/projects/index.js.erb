$("#pills-tabContent").append("<%= j(render(partial: "project", collection: @projects, as: :project)) %>");

<% if @projects.last_page? %>
  $("#load-more-pictures").hide();
<% else %>
  $("#load-more-pictures").attr("href", "<%= projects_path(page: (@projects.current_page + 1)) %>");
<% end %>
